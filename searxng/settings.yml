use_default_settings: true
server:
  secret_key: "openclaw-searxng-2026"
  bind_address: "0.0.0.0"
  port: 8080
search:
  formats:
    - html
    - json
  autocomplete: "google"
general:
  enable_metrics: false
