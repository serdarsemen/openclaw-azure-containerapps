.env
optillm.env
openclaw-repo/
*.openclaw-backup-*/
*.openclaw-staging-*/
openclaw-data/
upstream/
SMOKE-TEST.md
$null

# Generated at deploy time — embeds the plaintext gateway token / API keys
docker-compose-wsl.yaml
ollama-data/

# GitHub Actions runtime seed — may contain credentials/sessions.
# Force-add into a PRIVATE repo only: git add -f seed/openclaw-seed.tar.gz
seed/
.worktrees/wsl-deploy-optimization
